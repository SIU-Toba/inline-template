<!DOCTYPE html>
<html>
    <head>
        <title>Mi sitio web</title>
    </head>
    <body>
        <ul id="navigation">
        {% for item in navigation %}
            <li><a href="{{ item.href }}">{{ item.caption }}</a></li>
        {% endfor %}
        </ul>
        {{ inline_img('main.jpeg') }}
        {{ inline_js('main.js') }}
        {{ inline_css('main.css') }}
        
        <h1>Test-My Webpage</h1>
        {{ a_variable }}
    </body>
</html>